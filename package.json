{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "npmClient": "yarn",
  "useWorkspaces": true,
  "scripts": {
    "test": "lerna test",
    "version": "lerna version --conventional-commits --yes",
    "diff": "lerna diff"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "dependencies": {
    "react": "^16.13.1"
  }
}
